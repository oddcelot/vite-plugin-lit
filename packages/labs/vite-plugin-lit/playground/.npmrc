# The published @oddsquad/vite-plugin-lit may not yet declare a widened
# peer range (^7.0.0 || ^8.0.0); skip strict peer resolution to avoid
# install failures in standalone (StackBlitz/bolt.new) contexts.
legacy-peer-deps=true
