---
title: Find out why a component re-rendered
description: 'Which Lit components re-rendered, how often, how long for, and which reactive properties changed to cause it.'
sidebar:
  order: 4
  label: Updates
---

import {Aside, CardGrid, Code, LinkCard} from '@astrojs/starlight/components';
import context from '../../../../../../playground/src/hmr-context.ts?raw';
import {stripHeader} from '../../../../lib/fixture';
import ThemedImage from '../../../../components/ThemedImage.astro';
import devtoolsUpdatesTableLight from '../../../../assets/shots/devtools-updates-table.light.png';
import devtoolsUpdatesTableDark from '../../../../assets/shots/devtools-updates-table.dark.png';
import devtoolsUpdatesDetailLight from '../../../../assets/shots/devtools-updates-detail.light.png';
import devtoolsUpdatesDetailDark from '../../../../assets/shots/devtools-updates-detail.dark.png';

Answer "which component re-renders too much" and "why did this one update" from
one recording. Only the Lit Lifecycle layer needs to be on.

## Record one

1. Open the **Timeline** tab and press **▶ Record**.
2. Interact with the page, or just wait if something updates on its own.
3. Switch to **Updates**.

Recording is shared state, so it does not matter which tab you start it from.
Stopping keeps the recording. Starting again clears it, because the page
re-zeroes its timeline clock each time.

## Read the component table

The upper pane holds one row per component **tag**, slowest first.

<ThemedImage
  light={devtoolsUpdatesTableLight}
  dark={devtoolsUpdatesTableDark}
  alt="The Updates tab table: one row per component with update counts and total time."
/>

| Column    | Meaning                                                       |
| --------- | ------------------------------------------------------------- |
| Component | The tag, with `×n` when more than one instance of it updated. |
| Reasons   | The reactive properties that changed, most frequent first.    |
| Updates   | How many update ticks were recorded.                          |
| Total     | Summed time in `performUpdate` across those ticks.            |
| Max       | The slowest single update.                                    |

Rows group by tag rather than by instance on purpose. A list of 200 rows that
each updated twice is one component's problem, not two hundred.

Time comes from `performUpdate` alone. `willUpdate`, `update` and `updated` all
run inside it, so adding the phases together would count the same milliseconds
several times over. A tick whose end was not recorded, because it is still
running or older than the retained window, counts toward **Updates** but adds
nothing to **Total**.

## Read one component's updates

Pick a component and the lower pane lists each of its update ticks: **when** it
started and **how long** it took, **what changed**, **what caused it**, and the
instance id, which links through to the
[Components tab](/vite-plugin-lit/guides/devtools/components/).

<ThemedImage
  light={devtoolsUpdatesDetailLight}
  dark={devtoolsUpdatesDetailDark}
  alt="One update expanded in the Updates tab, showing the changed property keys and the triggering input event."
/>

The changed-property keys are the actual answer to "why did this re-render".
They come from Lit's own `changedProperties` map, so they are what the element
reacted to, not an inference.

In the playground fixture below, clicking the provider's button bumps the
marked `counter`. Both the provider and its consumer show a tick, and both name
`counter` under Reasons.

<Code
  code={stripHeader(context)}
  lang="ts"
  title="src/hmr-context.ts"
  mark={[23, 24, 25]}
/>

<Aside type="note" title="An update with no cause">
  Cause attribution reads the Mouse and Keyboard layers, which are off by
  default, and only claims an event that happened within 200ms before the
  update. An update with no cause is a timer, a signal, or a stray
  `requestUpdate()`. That is itself the useful finding, and better than blaming
  the last click that happened to be nearby.
</Aside>

A component that overrides `willUpdate` or `updated` without calling `super`
shadows the base implementation and does not report that phase. The tick still
appears, because `performUpdate` is never overridden, but its changed keys can
be missing.

## Link to a view

The tab is addressable like the others. The element id selects the component
row that instance belongs to, so the same id works whether you came from the
Components tree or from an exported snapshot.

```text frame="terminal"
#tab=updates&component=7
```

## Ask an agent instead

The same derivation is available over MCP as `lit_update-summary`, which a
[coding agent](/vite-plugin-lit/guides/devtools/agents/) should prefer over
`lit_recent-events` for anything shaped like "why did this re-render".

## Related

<CardGrid>
  <LinkCard
    title="Limitations"
    description="What the plugin cannot patch, when a reload explains the reset counts."
    href="/vite-plugin-lit/reference/limitations/"
  />
  <LinkCard
    title="Inspect live components"
    description="The tab an instance id links through to."
    href="/vite-plugin-lit/guides/devtools/components/"
  />
</CardGrid>
