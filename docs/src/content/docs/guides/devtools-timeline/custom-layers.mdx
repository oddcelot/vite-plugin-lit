---
title: Custom layers
description: Emit your own events onto the Lit timeline from app code or another Vite plugin.
sidebar:
  order: 7
---

import {CardGrid, LinkCard} from '@astrojs/starlight/components';

You want this when your app has its own events worth lining up against Lit's —
a router navigation, a websocket message, a store commit — and you would rather
see them on the same time axis than in the console.

## Register a layer, emit events

App code and other plugins can emit events on custom layers:

```ts
import {addTimelineEvent, addTimelineLayer} from 'virtual:lit-plugin/timeline';

// Register once (idempotent)
addTimelineLayer({id: 'my-layer', label: 'My Events', color: 0xff6b35});

// Emit from anywhere in the app
addTimelineEvent({
  layerId: 'my-layer',
  time: performance.now(),
  title: 'something happened',
  data: {detail: 42},
});
```

The virtual module resolves to a no-op stub in any `vite build` — including a
build with `timeline` enabled, since the events only ever travel over the dev
server's HMR channel — and in dev when `timeline` is disabled. Imports are safe
to leave in component code.

`color` is a number, not a CSS string — `0xff6b35` rather than `'#ff6b35'`.
`data` is whatever you want the event's detail pane to show; it is serialized,
so keep it to plain values.

A layer registered before the panel (or an agent) connects is still there once
it does — the dev server keeps registered layers in its session state rather
than relaying them to whoever happens to be listening at the time.

TypeScript picks the module up from `@oddsquad/vite-plugin-lit/client`; see
[import queries](/vite-plugin-lit/reference/import-queries/#virtuallit-plugintimeline)
for how to reference it.

## Related

<CardGrid>
  <LinkCard
    title="DevTools timeline"
    description="Setup, the built-in layers, and the Components tab."
    href="/vite-plugin-lit/guides/devtools-timeline/"
  />
  <LinkCard
    title="Runtime API"
    description="The virtual module's exports in full."
    href="/vite-plugin-lit/reference/runtime-api/"
  />
</CardGrid>
